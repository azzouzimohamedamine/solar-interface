Solar-Interface :

This project includes an interactive web interface.

To use:
1. Open index.html
2. Upload the CSV file "datase-solar.csv"
3. Get prediction results
Use the exact same dataset format (same columns).
